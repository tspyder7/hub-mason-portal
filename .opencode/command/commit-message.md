---
description: Generate a conventional commit message from staged changes and get a copyable git commit command.
---

Use the `commit-message` skill. Invoke the skill and follow its instructions exactly.
